# mock is deterministic and needs no running core; live requires an auth token.
VITE_VEQRI_MODE=mock
VITE_VEQRI_CORE_URL=http://127.0.0.1:7342
# Development browser fallback only. The native shell must inject keychain-backed auth.
VITE_VEQRI_DEV_TOKEN=replace-with-a-local-development-token
